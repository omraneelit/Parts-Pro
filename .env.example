# Copy to .env and set the shared backend URL. Note the /api suffix — all
# routes are mounted under the backend's APIRouter(prefix="/api").
EXPO_PUBLIC_API_URL=https://backend-earnest-glow-6275.fly.dev/api
